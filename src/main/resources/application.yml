spring:
  application:
    name: veras-api

  datasource:
    url: jdbc:mysql://localhost:3306/veras_db
    username: root
    password: emilly22
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

gemini:
  api-key: AIzaSyDPqQ9XuhZTe8MLErvkTeW9siVO_0oEKgE
  model: gemini-2.5-flash